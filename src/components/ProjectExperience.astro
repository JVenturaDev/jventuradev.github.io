<section class="section" id="experience" aria-labelledby="experience-title">
  <div class="container">
    <div class="section-heading">
      <div>
        <p class="eyebrow">Demonstrated through projects</p>
        <h2 id="experience-title">Engineering experience</h2>
      </div>
      <p>Four recurring disciplines connect the applications, libraries, and interfaces above.</p>
    </div>

    <ol class="experience-list">
      <li>
        <span>01</span>
        <div><h3>Application Architecture</h3><p>Clear boundaries across interfaces, domain logic, and persistence.</p></div>
      </li>
      <li>
        <span>02</span>
        <div><h3>Library Design</h3><p>Stable contracts, typed APIs, and reusable package surfaces.</p></div>
      </li>
      <li>
        <span>03</span>
        <div><h3>Testing &amp; CI</h3><p>Automated checks that make builds and releases repeatable.</p></div>
      </li>
      <li>
        <span>04</span>
        <div><h3>Cross-platform UI</h3><p>Responsive product work spanning browser and mobile contexts.</p></div>
      </li>
    </ol>
  </div>
</section>

<style>
  .experience-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin: 0;
    padding: 0;
    border-top: 1px solid var(--color-border-strong);
    list-style: none;
  }

  .experience-list li {
    display: grid;
    grid-template-columns: 2rem minmax(0, 1fr);
    gap: var(--space-4);
    padding: var(--space-6) var(--space-5) var(--space-6) 0;
    border-bottom: 1px solid var(--color-border);
  }

  .experience-list li:nth-child(odd) {
    margin-right: var(--space-5);
  }

  .experience-list span {
    color: var(--color-accent-strong);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
  }

  .experience-list h3,
  .experience-list p {
    margin-block: 0;
  }

  .experience-list h3 {
    margin-bottom: var(--space-2);
    font-size: var(--text-lg);
  }

  .experience-list p {
    color: var(--color-muted);
    font-size: var(--text-sm);
  }

  @media (max-width: 42rem) {
    .experience-list {
      grid-template-columns: 1fr;
    }

    .experience-list li:nth-child(odd) {
      margin-right: 0;
    }
  }
</style>
