<div class="technical-evidence" role="group" aria-label="polish-engine public API evidence">
  <section class="technical-evidence__panel" aria-labelledby="simple-evaluation">
    <p id="simple-evaluation">Simple evaluation</p>
    <pre><code><span class="code-keyword">const</span> engine = <span class="code-keyword">new</span> polishEngine();

<span class="code-keyword">const</span> result = engine.evaluate(<span class="code-string">"2 + 3 * 4"</span>);</code></pre>
    <div class="technical-evidence__result">
      <span>Result</span>
      <samp>14</samp>
    </div>
  </section>

  <section class="technical-evidence__panel" aria-labelledby="advanced-evaluation">
    <p id="advanced-evaluation">Advanced evaluation</p>
    <pre><code><span class="code-keyword">const</span> result = engine.evaluate(<span class="code-string">"sqrt(-4)"</span>, &#123;
  steps: <span class="code-value">true</span>,
  normalize: <span class="code-value">true</span>
&#125;);</code></pre>
    <div class="technical-evidence__result">
      <span>Normalized result</span>
      <samp>2i</samp>
    </div>
  </section>

  <section class="technical-evidence__panel technical-evidence__pipeline" aria-labelledby="expression-pipeline">
    <p id="expression-pipeline">Expression pipeline</p>
    <small>RPN means Reverse Polish Notation: operators follow their operands.</small>
    <ol>
      <li>Tokenizer</li>
      <li>Parser</li>
      <li>Postfix / RPN</li>
      <li>Evaluator</li>
      <li>Normalizer</li>
    </ol>
  </section>
</div>

<style>
  .technical-evidence {
    display: grid;
    gap: var(--space-4);
  }

  .technical-evidence__panel {
    min-width: 0;
    padding: var(--space-5);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-private-surface);
  }

  .technical-evidence p {
    margin: 0 0 var(--space-4);
    color: var(--color-muted);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  pre {
    margin: 0;
    overflow-x: auto;
    padding: var(--space-3);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    background: var(--color-code-surface);
    color: var(--color-text);
    font-family: var(--font-mono);
    font-size: clamp(0.72rem, 1.5vw, 0.82rem);
    line-height: 1.75;
  }

  .code-keyword { color: var(--color-accent-strong); }
  .code-string { color: var(--color-focus); }
  .code-value { font-weight: 800; }

  .technical-evidence__result {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
    margin-top: var(--space-4);
    padding-top: var(--space-3);
    border-top: 1px solid var(--color-border);
    color: var(--color-muted);
    font-size: var(--text-sm);
  }

  samp {
    color: var(--color-accent-strong);
    font-family: var(--font-mono);
    font-size: var(--text-xl);
    font-weight: 800;
  }

  ol {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-2);
    margin: 0;
    padding: 0;
    color: var(--color-text);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    list-style: none;
  }

  .technical-evidence__pipeline small {
    display: block;
    margin: calc(var(--space-2) * -1) 0 var(--space-4);
    color: var(--color-muted);
    font-size: var(--text-xs);
    line-height: 1.55;
  }

  li {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
  }

  li:not(:last-child)::after {
    color: var(--color-accent-strong);
    content: "\2192";
  }

</style>
