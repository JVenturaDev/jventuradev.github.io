<div class="technical-evidence" role="group" aria-label="polish-engine public API evidence">
  <div class="technical-evidence__example">
    <p>Public API example</p>
    <pre><code><span class="code-keyword">const</span> engine = <span class="code-keyword">new</span> polishEngine();

<span class="code-keyword">const</span> result = engine.evaluate(<span class="code-string">"sqrt(-4)"</span>, &#123;
  steps: <span class="code-value">true</span>,
  normalize: <span class="code-value">true</span>
&#125;);</code></pre>
    <div class="technical-evidence__result">
      <span>Normalized result</span>
      <samp>2i</samp>
    </div>
  </div>

  <div class="technical-evidence__pipeline">
    <p>Expression pipeline</p>
    <ol>
      <li>Tokenizer</li>
      <li>Parser</li>
      <li>Postfix / RPN</li>
      <li>Evaluator</li>
      <li>Normalizer</li>
    </ol>
  </div>
</div>

<style>
  .technical-evidence {
    display: grid;
    gap: 1px;
    overflow: hidden;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    background: var(--color-border);
  }

  .technical-evidence__example,
  .technical-evidence__pipeline {
    min-width: 0;
    padding: var(--space-5);
    background: var(--color-private-surface);
  }

  .technical-evidence p {
    margin: 0 0 var(--space-4);
    color: var(--color-muted);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  pre {
    margin: 0;
    overflow-x: auto;
    color: var(--color-text);
    font-family: var(--font-mono);
    font-size: clamp(0.72rem, 1.5vw, 0.82rem);
    line-height: 1.75;
  }

  .code-keyword { color: var(--color-accent-strong); }
  .code-string { color: var(--color-focus); }
  .code-value { font-weight: 800; }

  .technical-evidence__result {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-4);
    margin-top: var(--space-4);
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-border);
    color: var(--color-muted);
    font-size: var(--text-sm);
  }

  samp {
    color: var(--color-accent-strong);
    font-family: var(--font-mono);
    font-size: var(--text-xl);
    font-weight: 800;
  }

  ol {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-2);
    margin: 0;
    padding: 0;
    color: var(--color-text);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    list-style: none;
  }

  li {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
  }

  li:not(:last-child)::after {
    color: var(--color-accent-strong);
    content: "→";
  }

  @media (min-width: 38rem) {
    .technical-evidence {
      grid-template-columns: minmax(0, 1.2fr) minmax(12rem, 0.8fr);
    }

    .technical-evidence__pipeline ol {
      align-items: flex-start;
      flex-direction: column;
    }

    .technical-evidence__pipeline li:not(:last-child)::after {
      content: "↓";
    }
  }
</style>
